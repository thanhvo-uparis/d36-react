import styles from './Loading.module.scss';

function Loading() {
    return (
        <div className={styles.container}>
            <h3>Loading...</h3>
        </div>
    )
}

export default Loading;